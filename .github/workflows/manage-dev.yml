name: CD - Manage DEV Container (Manual)

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Welche Aktion soll ausgeführt werden?'
        required: true
        default: 'stop'
        type: choice
        options:
          - stop
          - start
          - restart

jobs:
  manage-container:
    runs-on: ubuntu-latest

    steps:
      - name: Manage Dev Container via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: devdeploy
          key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}
          port: 22
          script: |
            ACTION="${{ inputs.action }}"
            echo "Führe '$ACTION' für baerchenbot-dev-container aus..."

            if [ "$ACTION" = "stop" ]; then
              docker stop baerchenbot-dev-container || true
            elif [ "$ACTION" = "start" ]; then
              docker start baerchenbot-dev-container
            elif [ "$ACTION" = "restart" ]; then
              docker restart baerchenbot-dev-container
            fi
